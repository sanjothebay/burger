<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}} Eat This Burger Up!
		{{else}} Ate That Burger UP!{{/if}}
	</button>
	<button class="Delete-burger" data-id="{{id}}">
		DELELTE
	</button>
</li>