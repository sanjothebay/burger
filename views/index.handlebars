<img src="/public/assets/img/goodburger.JPG" alt="Burgerimg">
<h1>Burgers!</h1>

<h2>Burgers that have not devoured!</h2>

<ul>
    
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>

<form class="create-form">
    <div class="form-group">
        <label for="addburg">Burger Name:</label>
        <input type="text" id="addburg" name="name">
    </div>

    {{!-- <div class="form-group">
        <label for="devour">devoured</label><br>
        <input type="radio" name="devoured" value="1"> Checked devoured! <br>
        <input type="radio" name="devoured" value="0"> Devoured !
    </div> --}}


    <button type="submit"> Submit </button>

</form>